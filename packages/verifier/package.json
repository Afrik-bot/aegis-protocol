{
  "name": "@aegis-protocol/verifier",
  "version": "1.0.0",
  "description": "Aegis Protocol reference verifier — standalone HTTP trust decision service",
  "main": "dist/index.js",
  "bin": {
    "aegis-verifier": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/server.ts",
    "start": "node dist/server.js",
    "test": "jest --config jest.config.js"
  },
  "files": ["dist"],
  "license": "MIT",
  "dependencies": {
    "@aegis-protocol/spec": "^1.0.0",
    "@aegis-protocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "ts-node": "^10.9.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "@types/node": "^20.0.0"
  }
}
